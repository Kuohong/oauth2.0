<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.power4j.oauth2.dao.UserDao">
        <resultMap id="user" type="com.power4j.oauth2.common.pojo.User">
                <result column="id" property="id" jdbcType="INTEGER" />
                <result column="uuid" property="uuid" jdbcType="VARCHAR" />
                <result column="user_name" property="userName" jdbcType="VARCHAR"/>
                <result column="passsword" property="password" jdbcType="VARCHAR"/>
                <result column="nick_name" property="nickName" jdbcType="VARCHAR"/>
                <result column="real_name" property="realName" jdbcType="VARCHAR"/>
                <result column="status" property="status" typeHandler="com.power4j.oauth2.mybatis.handler.CommonStatusHandler" />
                <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        </resultMap>
        <sql id="Base_Column_List" >
                id, uuid,user_name,  password, nick_name, real_name, status, create_time
        </sql>
        <insert id="saveUser" parameterType="com.power4j.oauth2.common.pojo.User">
           insert into sys_user (uuid, user_name, passsword, nick_name, real_name, status, create_time)
           values (#{uuid}, #{userName}, #{password}, #{nickName}, #{realName}, #{status, typeHandler="com.power4j.oauth2.mybatis.handler.CommonStatusHandler}, #{createTime})
        </insert>
        <select id="selectUserByUserName"  resultMap="role">
          SELECT
          <include refid="Base_Column_List"/>
          FROM sys_user
          WHERE user_name = #{userName}
        </select>
        <select id="selectByUuId" resultMap="role">
            SELECT
            <include refid="Base_Column_List"/>
            FROM sys_user
            WHERE uuid = #{uuid}
        </select>
    <update id="updateUserStatus">
        UPDATE sys_user SET status = #{status, typeHandler=com.power4j.oauth2.mybatis.handler.CommonStatusHandler}
        where uuid = #{uuid}
    </update>

    <insert id="insertUserRole">
        INSERT INTO sys_user_role (user_uuid, role_uuid) values (#{userUUID}, #{roleUUID})
    </insert>
    <delete id="deleteUserRole">
        DELETE FROM sys_user_role WHERE user_uuid = #{uuid}
    </delete>

</mapper>
